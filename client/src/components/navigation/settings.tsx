import { useState } from "react"

const Settings = () => {
    const [hy, setHy]   = useState<number>(2);
    return (
        <div>
            <h1>Settings</h1>
        </div>
    )
}

export default Settings